# 🔧 حل مشكلة "Port 3000 already in use"

## ❌ المشكلة:
```
Error: listen EADDRINUSE: address already in use :::3000
```

## ✅ الحل:

### الطريقة 1: إيقاف العملية القديمة
```bash
# إيجاد العملية التي تستخدم المنفذ 3000
lsof -ti:3000

# إيقاف العملية
kill -9 $(lsof -ti:3000)
```

### الطريقة 2: استخدام منفذ آخر
```bash
# في backend/.env
PORT=3001

# أو في server.js
const PORT = process.env.PORT || 3001;
```

### الطريقة 3: إيقاف جميع عمليات Node
```bash
# إيقاف جميع عمليات Node
killall node

# أو
pkill -f node
```

---

## 🔍 التحقق:

```bash
# التحقق من أن المنفذ 3000 فارغ
lsof -ti:3000

# إذا لم يظهر شيء، المنفذ فارغ ✅

# التحقق من أن الخادم يعمل
curl http://localhost:3000/api/health
```

---

## 💡 نصائح:

1. **استخدم `npm run dev` مرة واحدة فقط**
2. **إذا أردت إيقاف الخادم:** اضغط `Ctrl+C` في Terminal
3. **للتحقق من العمليات:** `ps aux | grep node`

---

## ✅ بعد الحل:

```bash
cd backend
npm run dev
```

يجب أن يعمل الخادم بدون مشاكل! 🚀

