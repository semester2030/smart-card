# 🔧 إصلاح مشكلة OTP - Email غير مُعدّ

## ⚠️ **المشكلة:**

1. **Email غير مُعدّ**: `EMAIL_PASSWORD=YOUR_APP_PASSWORD_HERE`
2. **OTP لا يصل على الإيميل**: يظهر فقط في Backend Console
3. **الشاشة تعرض رقم الجوال**: تم إصلاحها لعرض الإيميل

---

## ✅ **ما تم إصلاحه:**

1. ✅ تحديث `otp_screen.dart` لعرض **Email** بدلاً من Phone
2. ✅ تغيير الأيقونة من `Icons.sms` إلى `Icons.email`

---

## 🔧 **ما يجب عليك فعله:**

### **1. الحصول على App Password:**

#### **خطوات سريعة:**
1. اذهب إلى: https://account.microsoft.com/security
2. اضغط "Advanced security options"
3. اضغط "App passwords"
4. أنشئ App Password جديد:
   - اختر "Mail" و "Other (custom name)"
   - اكتب: `Smart Card Backend`
   - اضغط "Generate"
5. انسخ App Password (16 حرف)

---

### **2. تحديث `.env`:**

افتح `backend/.env` وحدث:

```env
EMAIL_PASSWORD=your-app-password-here
```

**بدلاً من:**
```env
EMAIL_PASSWORD=YOUR_APP_PASSWORD_HERE
```

---

### **3. أعد تشغيل Backend:**

```bash
cd backend
npm run dev
```

---

## 🧪 **الاختبار:**

### **1. بعد تحديث `.env`:**

1. أعد تشغيل Backend
2. جرب التسجيل بإيميل جديد
3. يجب أن يصل OTP على الإيميل ✅

### **2. إذا لم يصل Email:**

1. تحقق من **Spam/Junk** folder
2. تحقق من Backend Console:
   - إذا ظهر `⚠️ Email not configured` → Email غير مُعدّ
   - إذا ظهر `✅ OTP email sent` → Email تم إرساله

---

## 📧 **ملاحظات:**

### **في Development (الآن):**
- إذا لم يكن Email مُعدّ، OTP سيظهر في console
- هذا كافي للاختبار

### **في Production:**
- يجب إعداد Email بشكل صحيح
- OTP سيصل على الإيميل

---

## ✅ **بعد الإعداد:**

1. ✅ أضف App Password في `.env`
2. ✅ أعد تشغيل Backend
3. ✅ جرب التسجيل
4. ✅ يجب أن يصل OTP على الإيميل

---

**جاهز! أضف App Password واختبر!** 🚀

