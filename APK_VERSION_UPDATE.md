# 🔄 تحديث رقم الإصدار - APK

## ❌ **المشكلة:**
APK الذي حمّلته هو نفس النسخة السابقة.

**السبب:** `versionCode` لم يتغير - Android لا يرى أنه تحديث.

---

## ✅ **الحل:**

تم تحديث رقم الإصدار في `pubspec.yaml`:

### **قبل:**
```yaml
version: 1.0.0+1
```

### **بعد:**
```yaml
version: 1.0.1+2
```

**شرح:**
- `1.0.1` = versionName (يظهر للمستخدم)
- `+2` = versionCode (يستخدمه Android لتحديد التحديثات)

---

## 📋 **الخطوات:**

### **1. تم تحديث pubspec.yaml:**
- ✅ `version: 1.0.1+2`

### **2. تم تنظيف المشروع:**
- ✅ `flutter clean`

### **3. تم بناء APK جديد:**
- ✅ `flutter build apk --release`

---

## ✅ **بعد البناء:**

APK الجديد:
- **الموقع:** `build/app/outputs/flutter-apk/app-release.apk`
- **الإصدار:** 1.0.1+2 (جديد!)
- **API URL:** `https://smart-card-api.railway.app/api` ✅

---

## 📱 **التثبيت:**

### **1. احذف التطبيق القديم (اختياري):**
- Settings → Apps → smart_card → Uninstall

### **2. ثبت APK الجديد:**
- انسخ `app-release.apk` إلى جهاز Android
- افتح الملف وثبته

---

## 🔄 **للتحديثات المستقبلية:**

عند بناء APK جديد، زد `versionCode`:
```yaml
version: 1.0.2+3  # زد الرقم بعد +
version: 1.0.3+4  # وهكذا...
```

---

**APK الجديد جاهز مع رقم إصدار جديد!** 🚀

