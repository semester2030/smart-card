# ⚠️ DATABASE_URL غير موجود!

## ❌ **المشكلة:**

في صفحة Variables، أرى:
- ✅ `DATABASE_PUBLIC_URL` (موجود)
- ✅ `PGDATABASE` (موجود)
- ❌ **`DATABASE_URL` غير موجود!** ← هذا ما نحتاجه!

---

## ✅ **الحل: إضافة DATABASE_URL**

### **الخطوات:**

#### **1. في صفحة Variables الحالية:**

1. اضغط على **"+ New Variable"** (الزر البنفسجي في الأعلى)
2. في حقل **"VARIABLE_NAME"**، اكتب: `DATABASE_URL`
3. اضغط على **"Add Reference"** (أو استخدم الأيقونة `{}`)
4. اختر **"Postgres"** service
5. اختر **"DATABASE_URL"** من القائمة (ليس DATABASE_PUBLIC_URL!)
6. اضغط **"Add"**

---

## ⚠️ **ملاحظة مهمة:**

- ❌ **لا تستخدم `DATABASE_PUBLIC_URL`** - هذا للوصول العام وقد يكلفك رسوم
- ✅ **استخدم `DATABASE_URL`** - هذا للوصول الخاص (مجاني)

---

## ✅ **بعد إضافة DATABASE_URL:**

1. Railway سيعيد تشغيل service تلقائياً
2. اذهب إلى **"Deploy Logs"** tab
3. يجب أن ترى:
   - `✅ PostgreSQL Connected: ...`
   - `✅ Database tables synced`

---

**أضف DATABASE_URL الآن!** 🚀

