# 🧪 دليل اختبار عملية التسجيل - Smart Card App

## 📋 **خطوات الاختبار:**

### **1. تشغيل Backend:**
```bash
cd backend
npm run dev
```

**يجب أن ترى:**
```
🚀 Server running on port 3000
✅ PostgreSQL Connected
✅ Database tables synced
```

---

### **2. تشغيل التطبيق على المحاكي:**

#### **iOS Simulator:**
```bash
flutter run -d EC42F1BF-89B6-479A-B3D7-5CDBF043325E
```

#### **أو Android Emulator:**
```bash
flutter run -d Medium_Phone_API_36.0
```

---

## 🔄 **عملية التسجيل الكاملة:**

### **الخطوة 1: فتح شاشة التسجيل**
1. افتح التطبيق
2. اضغط "إنشاء حساب" أو "Register"

---

### **الخطوة 2: ملء بيانات التسجيل**

#### **للزائر (Visitor):**
- **الاسم:** اختبار الزائر
- **البريد الإلكتروني:** visitor@test.com
- **رقم الجوال:** 0500123456
- **كلمة المرور:** 123456
- **الدور:** زائر (Visitor)
- **الاهتمامات:** (اختياري)

#### **للعارض (Exhibitor):**
- **الاسم:** اختبار العارض
- **اسم الشركة:** شركة الاختبار
- **البريد الإلكتروني:** exhibitor@test.com
- **رقم الجوال:** 0500987654
- **كلمة المرور:** 123456
- **الدور:** عارض (Exhibitor)

---

### **الخطوة 3: إرسال البيانات**
1. اضغط "إنشاء حساب" أو "Register"
2. يجب أن تظهر رسالة نجاح
3. سيتم الانتقال تلقائياً إلى شاشة OTP

---

### **الخطوة 4: الحصول على OTP**

**في Backend Terminal، يجب أن ترى:**
```
OTP for visitor@test.com: 123456
```

**ملاحظة:** OTP يظهر في console الخادم (للاختبار فقط)

---

### **الخطوة 5: إدخال OTP**
1. في شاشة OTP، أدخل الرمز من console
2. اضغط "تحقق" أو "Verify"
3. يجب أن يتم تسجيل الدخول تلقائياً

---

### **الخطوة 6: التحقق من النجاح**
1. يجب أن تنتقل إلى Dashboard المناسب:
   - **للزائر:** Visitor Dashboard
   - **للعارض:** Exhibitor Dashboard
2. يجب أن ترى بيانات المستخدم

---

## 🔍 **ما يجب مراقبته:**

### **في Backend Terminal:**
- ✅ Request received: `POST /api/auth/register`
- ✅ OTP generated: `OTP for email: XXXXXX`
- ✅ Request received: `POST /api/auth/verify-otp`
- ✅ User verified successfully

### **في Flutter App:**
- ✅ Loading indicator أثناء التسجيل
- ✅ رسالة نجاح بعد التسجيل
- ✅ الانتقال إلى شاشة OTP
- ✅ Loading indicator أثناء التحقق
- ✅ الانتقال إلى Dashboard

---

## ⚠️ **المشاكل المحتملة:**

### **1. "Connection refused":**
**السبب:** Backend غير مشغل
**الحل:** شغّل `npm run dev` في backend

### **2. "Route not found":**
**السبب:** baseUrl خاطئ
**الحل:** تأكد من `http://localhost:3000/api` للمحاكي

### **3. "OTP غير صحيح":**
**السبب:** OTP منتهي الصلاحية أو خاطئ
**الحل:** استخدم OTP من console الخادم

### **4. "البريد الإلكتروني مستخدم":**
**السبب:** المستخدم مسجل مسبقاً
**الحل:** استخدم email جديد أو احذف المستخدم من Database

---

## 📊 **التحقق من Database:**

### **للتحقق من المستخدم المسجل:**
```bash
psql smartcard -c "SELECT name, email, role, \"isVerified\" FROM users;"
```

### **للتحقق من OTP:**
```bash
psql smartcard -c "SELECT email, \"otpCode\", \"otpExpiresAt\" FROM users WHERE \"isVerified\" = false;"
```

---

## ✅ **قائمة التحقق:**

- [ ] Backend يعمل
- [ ] التطبيق يعمل على المحاكي
- [ ] يمكن فتح شاشة التسجيل
- [ ] يمكن ملء البيانات
- [ ] يمكن إرسال البيانات
- [ ] OTP يظهر في console
- [ ] يمكن إدخال OTP
- [ ] يمكن التحقق من OTP
- [ ] يتم تسجيل الدخول تلقائياً
- [ ] يتم الانتقال إلى Dashboard

---

## 🎯 **بعد الاختبار:**

إذا نجح كل شيء:
- ✅ عملية التسجيل تعمل بشكل صحيح
- ✅ Backend متصل ويعمل
- ✅ Database يحفظ البيانات
- ✅ Authentication يعمل

---

**جاهز للاختبار!** 🚀

