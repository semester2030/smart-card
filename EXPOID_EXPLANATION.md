# 🎫 شرح ExpoID - الرقم الخاص لكل مستخدم

## 📋 نظرة عامة

**ExpoID** هو رقم فريد يتم تعيينه تلقائياً لكل مستخدم (زائر أو عارض) في تطبيق Smart Card. يعمل كمعرف فريد ووسيلة للتواصل بين المستخدمين.

---

## 🔢 ما هو ExpoID؟

### الصيغة:
```
Expo#XXXX
```
- **Expo#**: بادئة ثابتة
- **XXXX**: 4 أرقام عشوائية (1000-9999)

### أمثلة:
- `Expo#1200` - زائر
- `Expo#2048` - عارض
- `Expo#3056` - عارض آخر

---

## 🤖 كيف يتم إنتاج ExpoID؟

### ✅ **تلقائياً (Automatic)**

ExpoID يتم توليده **تلقائياً** عند التسجيل، وليس يدوياً.

#### في الموقع القديم (JavaScript):
```javascript
// js/app.js - السطر 231
function generateExpoID() {
    const num = Math.floor(Math.random() * 9000) + 1000;
    return `Expo#${num}`;
}
```

#### في التطبيق الجديد (Flutter):
```dart
// lib/utils/helpers.dart
static String generateExpoId() {
  final random = DateTime.now().millisecondsSinceEpoch;
  final id = (random % 10000).toString().padLeft(4, '0');
  return '${AppConstants.expoIdPrefix}$id';
}
```

#### في AuthProvider:
```dart
// lib/providers/auth_provider.dart - السطر 149
final expoId = 'Expo#${1000 + DateTime.now().millisecondsSinceEpoch % 9000}';
```

### 📝 **متى يتم التوليد؟**

1. **عند التسجيل**: يتم توليد ExpoID تلقائياً عند إنشاء حساب جديد
2. **عند Demo Mode**: يتم تعيين ExpoID افتراضي (مثل `Expo#1200` للزائر، `Expo#2048` للعارض)

---

## 🎯 دور ExpoID

### 1. **معرف فريد (Unique Identifier)**
- يميز كل مستخدم عن الآخرين
- يستخدم في قاعدة البيانات كمعرف
- لا يمكن تكراره (يتم التحقق من التكرار)

### 2. **QR Code للعارض**
- كل عارض يحصل على QR Code يحتوي على ExpoID الخاص به
- الزائر يمسح QR Code للحصول على ExpoID العارض
- يتم حفظ جهة الاتصال تلقائياً

### 3. **البحث والتواصل**
- يمكن البحث عن مستخدم باستخدام ExpoID
- يمكن استخدام ExpoID لتسجيل الدخول (بدلاً من البريد الإلكتروني)
- يتم استخدام ExpoID في طلبات التواصل

### 4. **ربط البيانات**
- ربط الملاحظات بجهات الاتصال (contactExpoId)
- ربط Leads بالزوار (visitorExpoId)
- ربط طلبات التواصل (visitorExpoId, exhibitorExpoId)

---

## 📱 كيف يتم التواصل من خلال ExpoID؟

### السيناريو الكامل:

#### 1. **العارض يحصل على ExpoID**
```
عند التسجيل:
- العارض يسجل حساب جديد
- النظام يولد ExpoID تلقائياً: Expo#2048
- يتم حفظه في الملف الشخصي
- يتم إنشاء QR Code يحتوي على ExpoID
```

#### 2. **الزائر يمسح QR Code**
```
في المعرض:
- العارض يعرض QR Code في الجناح
- الزائر يفتح التطبيق ويمسح QR Code
- التطبيق يقرأ ExpoID من QR Code: Expo#2048
- يتم البحث عن العارض باستخدام ExpoID
- يتم عرض تفاصيل العارض (الشركة، الوصف، البروشور)
```

#### 3. **الزائر يحفظ جهة الاتصال**
```
بعد المسح:
- الزائر يضغط "حفظ جهة الاتصال"
- يتم حفظ:
  - ExpoID العارض: Expo#2048
  - اسم الشركة: نقل بلس
  - تفاصيل أخرى
- يتم ربط الملاحظات والمتابعات بـ ExpoID
```

#### 4. **الزائر يرسل طلب تواصل**
```
من لوحة التحكم:
- الزائر يختار جهة اتصال
- يضغط "إرسال طلب تواصل"
- يتم إنشاء Request يحتوي على:
  - visitorExpoId: Expo#1200
  - exhibitorExpoId: Expo#2048
  - الرسالة
```

#### 5. **العارض يستقبل الطلب**
```
في لوحة تحكم العارض:
- العارض يرى طلبات التواصل
- كل طلب يحتوي على:
  - visitorExpoId: Expo#1200
  - اسم الزائر
  - الرسالة
- العارض يقبل أو يرفض الطلب
```

---

## 🔍 استخدامات ExpoID في التطبيق

### 1. **البحث عن المستخدمين**
```dart
// lib/services/mock_api_service.dart
Future<UserModel?> getUserByEmailOrExpoId(String emailOrExpoId) async {
  // يمكن البحث بالبريد الإلكتروني أو ExpoID
  if (visitor.expoId == emailOrExpoId) return visitor;
  if (exhibitor.expoId == emailOrExpoId) return exhibitor;
}
```

### 2. **تسجيل الدخول**
```dart
// lib/screens/auth/login_screen.dart
// يمكن تسجيل الدخول باستخدام:
// - البريد الإلكتروني
// - ExpoID
CustomTextInput(
  label: 'البريد الإلكتروني أو ExpoID',
  ...
)
```

### 3. **التحقق من صحة ExpoID**
```dart
// lib/utils/helpers.dart
static bool isValidExpoId(String expoId) {
  if (!expoId.startsWith('Expo#')) return false;
  final id = expoId.replaceFirst('Expo#', '');
  if (id.length != 4) return false;
  return int.tryParse(id) != null;
}
```

### 4. **في QR Code Scanning**
```dart
// lib/screens/shared/scan_screen.dart
// عند مسح QR Code:
if (!Helpers.isValidExpoId(code)) {
  // ExpoID غير صحيح
  return;
}
// البحث عن جهة اتصال باستخدام ExpoID
final contact = contacts.firstWhere((c) => c.expoId == code);
```

---

## 🎨 عرض ExpoID في الواجهة

### في لوحة تحكم الزائر:
```dart
// lib/screens/visitor_dashboard/visitor_home.dart
Text(
  authProvider.currentUser?.expoId ?? 'Expo#1200',
  style: TextStyle(fontSize: 14, color: Colors.grey),
)
```

### في لوحة تحكم العارض:
```dart
// lib/screens/exhibitor_dashboard/exhibitor_home.dart
Text(
  authProvider.currentUser?.expoId ?? 'Expo#2048',
  style: TextStyle(fontSize: 14, color: Colors.grey),
)
```

### في بطاقة جهة الاتصال:
```dart
// lib/widgets/cards/contact_card.dart
Text(
  contact.expoId,
  style: TextStyle(fontSize: 12, color: Colors.grey),
)
```

---

## 🔐 الأمان والخصوصية

### 1. **ExpoID ليس سراً**
- ExpoID يمكن مشاركته علناً (في QR Code)
- لا يحتوي على معلومات حساسة
- لا يمكن استخدامه للوصول غير المصرح به

### 2. **التحقق من الهوية**
- تسجيل الدخول يحتاج كلمة المرور (حتى مع ExpoID)
- لا يمكن تغيير ExpoID بعد التسجيل
- كل ExpoID مرتبط بحساب واحد فقط

### 3. **الخصوصية**
- ExpoID لا يكشف معلومات شخصية
- يمكن استخدامه للتواصل بدون كشف الهوية الكاملة
- يمكن حذف ExpoID من البيانات المحفوظة

---

## 📊 إحصائيات ExpoID

### النطاق:
- **الحد الأدنى**: Expo#1000
- **الحد الأقصى**: Expo#9999
- **إجمالي الأرقام المتاحة**: 9000 رقم

### التوزيع:
- **للزوار**: عادة من Expo#1000 إلى Expo#1999
- **للعارضين**: عادة من Expo#2000 إلى Expo#9999
- **لا يوجد قواعد صارمة** - التوزيع عشوائي

---

## 🚀 التطوير المستقبلي

### 1. **ExpoID مخصص**
- إمكانية اختيار ExpoID مخصص (إذا كان متاحاً)
- دفع إضافي للحصول على رقم مميز

### 2. **QR Code ديناميكي**
- QR Code يتجدد تلقائياً
- إمكانية تعطيل QR Code مؤقتاً

### 3. **تحليلات ExpoID**
- تتبع عدد مرات مسح QR Code
- إحصائيات حول استخدام ExpoID

### 4. **ExpoID للمنظمين**
- ExpoID خاص للمنظمين (مثل Expo#0001)
- صلاحيات إضافية

---

## 📝 ملخص

✅ **ExpoID يتم توليده تلقائياً** عند التسجيل
✅ **لا يمكن تعديله** بعد التسجيل
✅ **يستخدم في QR Code** للعارضين
✅ **يستخدم للبحث والتواصل** بين المستخدمين
✅ **معرف فريد** لكل مستخدم
✅ **آمن وخصوصي** - لا يكشف معلومات حساسة

---

## 💡 أمثلة عملية

### مثال 1: زائر يمسح QR Code
```
1. العارض يعرض QR Code: Expo#2048
2. الزائر يمسح QR Code
3. التطبيق يقرأ: Expo#2048
4. يتم البحث عن العارض
5. يتم عرض تفاصيل الشركة
6. الزائر يحفظ جهة الاتصال
```

### مثال 2: زائر يبحث عن عارض
```
1. الزائر يكتب في البحث: Expo#2048
2. التطبيق يبحث عن ExpoID
3. يتم العثور على العارض
4. يتم عرض تفاصيله
```

### مثال 3: طلب تواصل
```
1. الزائر (Expo#1200) يرسل طلب تواصل
2. العارض (Expo#2048) يستقبل الطلب
3. العارض يقبل الطلب
4. يتم ربط الطلب بـ ExpoID كلا الطرفين
```

---

## 🎯 الخلاصة

**ExpoID** هو نظام ذكي للتعريف والتواصل في تطبيق Smart Card:
- ✅ توليد تلقائي
- ✅ فريد لكل مستخدم
- ✅ يستخدم في QR Code
- ✅ وسيلة للتواصل
- ✅ آمن وخصوصي

